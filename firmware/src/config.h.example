#ifndef CONFIG_H
#define CONFIG_H

// WiFi credentials
#define WIFI_SSID "Home"
#define WIFI_PASSWORD "Airport25"

// Server connection
#define SERVER_HOST "192.168.50.132"
#define SERVER_PORT 5000

// HX711 pins (ESP32-C3)
#define HX711_DOUT_PIN 4  // DT/DOUT pin (changed from 2)
#define HX711_SCK_PIN 5   // SCK pin (changed from 3)

// Sampling configuration
#define SAMPLE_RATE_HZ 80  // Match HX711 native rate

// LED indicator (optional - GPIO 8 is usually onboard LED)
#define STATUS_LED_PIN 8
#define USE_STATUS_LED true  // Set to false if no LED

#endif
